---
- name: update teleport
  hosts: servers
  strategy: free

  pre_tasks:
    - name: "APT | Update & Upgrade"
      become: yes
      apt:
        upgrade: yes
        update_cache: yes

  roles:
    - teleport
